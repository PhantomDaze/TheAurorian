# 同步 1.20.1 分支缺失的修复到 1.19.2

## 来源：1.20.1 最近 6 次提交
1. `ef4be938 fix(loot)` — weeping willow/tallgrass 掉落
2. `706ebc67 fix(locator)` — 网络同步粒子 + search radius
3. `85ff40b9 fix(entity)` — 远程攻击起点/瞄准
4. `0569d19c fix(resources)` — track crystalline_sprite
5. `abcd7159 fix(patchouli)` — 闭合 $(br2)
6. `08a310e7 docs(plan)` — 任务文档

## 1.19.2 现状核对
- ① loot：已由 `1e85599c` 同步（且更完善：含 bright_bulb/lavender/petunia/silkberry 草）。✅ 跳过
- ④ crystalline_sprite：已由 `f57baf53` 同步（generated 已追踪）。✅ 跳过
- ⑥ docs：纯任务追踪文档，不移植。✅ 跳过

## 待移植（3 项）

### A. patchouli 闭合 $(br2) — 来自 abcd7159
- `assets/theaurorian/patchouli_books/the_aurorian_guide/zh_cn/entries/aurorianbasics/6moonlightforge.json` 行 22：`$(br2融` → `$(br2)融`
- `assets/theaurorian/patchouli_books/the_aurorian_guide/zh_cn/entries/modprogression/2therunestonedungeon.json` 行 17：`$(br2本` → `$(br2)本`
- 注：1.19.2 路径在 `assets/` 下，1.20.1 在 `data/` 下，内容一致。

### B. entity 远程攻击修正 — 来自 85ff40b9
- `CrystallineBeamEntity`：`setPos(x, getEyeY()-0.3, z)` → `setPos(x, getY()+getBbHeight()*0.5, z)`（包围盒中心）
- `CrystallineSpriteEntity`：去掉 `d3*0.2` 弧度补偿，直瞄目标胸
- `DungeonKeeperEntity`：新增 `performRangedAttack` override，箭起点 `getY()+getBbHeight()-0.5`
- **1.19.2 适配**：1.20.1 用 `this.level()`，1.19.2 用 `this.level` 字段。其它 API 一致。

### C. locator 网络粒子 — 来自 706ebc67
- 新建 `Network/LocatorNetwork.java`（SimpleChannel + DungeonDirectionMessage）
- 新建 `Items/DungeonLocatorClient.java`（@OnlyIn(CLIENT) 客户端粒子）
- 改 `DungeonLocatorItem`：删除 spawnDirectionParticles，改用 `LocatorNetwork.CHANNEL.send`；searchRadius 去掉 `*16`（返回 6/4/2）
- 改 `TheAurorian`：注册 `LocatorNetwork.register()`
- **1.19.2 适配**：`record` 在 Java 17 支持（项目已用 switch 表达式）；`PacketDistributor.PLAYER.with` API 一致；`findNearestMapStructure` 签名不变。

## 验证
- `export JAVA_HOME=/usr/lib/jvm/zulu-17`
- `sh ./gradlew compileJava` 编译通过
- 运行时：粒子/远程攻击/搜索半径逻辑改动需游戏内验证（本次只保证编译 + 资源正确）

## 状态：已完成 ✅
- compileJava BUILD SUCCESSFUL。
- A patchouli：两个文件 $(br2) 闭合（与 1.20.1 abcd7159 一致）。
- B entity：Beam 起点→包围盒中心；Sprite 去弧度补偿直瞄胸；Keeper 新增 performRangedAttack 箭起点头部。1.19.2 适配 level 字段。
- C locator：新建 LocatorNetwork（SimpleChannel+record 消息）+ DungeonLocatorClient（@OnlyIn CLIENT）；DungeonLocatorItem 删本地 spawnDirectionParticles 改发包，searchRadius 去 *16；TheAurorian 注册通道。
  - 修正：1.19.2 无 net.minecraft.core.registries.Registries，删除误加 import，沿用 Registry.STRUCTURE_REGISTRY。

## 搁置
- locator 实际粒子显示/地牢搜索半径需游戏内验证。
- keeper performRangedAttack 的 RangedBowGoal 与原有 KeeperRangedGoal/KeeperBarrageGoal 并存是否会双重射击，需游戏内观察（1.20.1 同样如此，未额外处理）。

